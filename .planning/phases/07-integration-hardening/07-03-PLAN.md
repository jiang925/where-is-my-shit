---
phase: 07
wave: 2
depends_on:
  - 07-02
autonomous: true
files_modified:
  - tests/integration/test_api_auth.py
  - tests/integration/conftest.py
---

# Phase 7, Part 3: Integration Testing

## Goal
Verify the end-to-end authentication flow using an automated integration test suite. This ensures that the API enforces security and clients can successfully authenticate.

## Context
We have implemented auth in the API (Phase 6) and clients (Phase 7 Part 1 & 2). Now we need to prove they work together without manual testing.

## Tasks

<task type="auto">
<name>Create Integration Test Fixture</name>
<files>tests/integration/conftest.py</files>
<action>
Create `tests/integration/conftest.py` (or update existing):
1. Define a `live_server` fixture that:
   - Sets `WIMS_PASSWORD` env var to "test-password".
   - Starts the FastAPI app (`src.app.main:app`) using `uvicorn` in a separate thread/process on a free port.
   - Waits for `/health` endpoint to be ready.
   - Tears down server after tests.
</action>
<verify>
pytest tests/integration/test_api_auth.py
</verify>
<done>false</done>
</task>

<task type="auto">
<name>Implement Auth Scenarios</name>
<files>tests/integration/test_api_auth.py</files>
<action>
Create `tests/integration/test_api_auth.py`:
1. **Scenario A (Happy Path)**:
   - Initialize `WimsClient` with correct password.
   - Call `client.login()`.
   - Verify `client.token` is set.
   - Call `client.ingest(...)`.
   - Verify server returns 200 OK.
2. **Scenario B (Bad Password)**:
   - Initialize `WimsClient` with wrong password.
   - Call `client.login()`.
   - Verify it raises `AuthError`.
3. **Scenario C (No Auth)**:
   - Make raw HTTP request to `/api/v1/ingest` without header.
   - Verify 401 Unauthorized.
</action>
<verify>
pytest tests/integration/test_api_auth.py
</verify>
<done>false</done>
</task>

<task type="auto">
<name>Verify Token Refresh (Mocked)</name>
<files>tests/integration/test_api_auth.py</files>
<action>
Add Scenario D to `tests/integration/test_api_auth.py`:
1. Authenticate successfully.
2. Manually set `client.token` to an expired/invalid string.
3. Call `client.ingest(...)`.
4. Verify client catches 401, calls `login()` again, updates token, and retries request successfully.
</action>
<verify>
pytest tests/integration/test_api_auth.py
</verify>
<done>false</done>
</task>

## Must Haves
- Integration tests run against a real running server instance (not just mocked FastAPI app).
- Tests cover login success, login failure, and unauthenticated access rejection.
- Client auto-refresh logic is verified.
