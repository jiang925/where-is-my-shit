---
phase: 01-core-engine
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/schemas/message.py
  - src/app/services/embedding.py
  - src/app/db/client.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Embedding service generates vectors from text"
    - "Database client connects to LanceDB"
    - "Table schema matches requirements (content, vector, metadata)"
  artifacts:
    - path: "src/app/db/client.py"
      provides: "LanceDB connection management"
      exports: ["get_db_connection", "init_db"]
    - path: "src/app/services/embedding.py"
      provides: "Text to vector conversion"
      exports: ["EmbeddingService"]
    - path: "src/app/schemas/message.py"
      provides: "Data models"
      contains: "class Message"
  key_links:
    - from: "src/app/db/client.py"
      to: "src/app/schemas/message.py"
      via: "LanceModel schema definition"
    - from: "src/app/services/embedding.py"
      to: "fastembed"
      via: "TextEmbedding model"
---

<objective>
Implement the core data persistence layer and embedding intelligence.

Purpose: Enable the application to understand semantic meaning (embeddings) and store/retrieve data efficiently (LanceDB).
Output: A working database client and embedding service ready for API integration.
</objective>

<execution_context>
@.planning/phases/01-core-engine/01-02-PLAN.md
</execution_context>

<context>
@.planning/phases/01-core-engine/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Data Schemas</name>
  <files>src/app/schemas/message.py</files>
  <action>
    Create `src/app/schemas/message.py`.
    Define Pydantic/LanceModel models:

    1. `Message(LanceModel)`:
       - id: str
       - conversation_id: str
       - platform: str
       - title: str
       - content: str (func=create_index(fts=True))
       - role: str
       - timestamp: datetime
       - url: str
       - vector: Vector(384) (func=create_index(metric="cosine"))

    2. `IngestRequest(BaseModel)`:
       - Payload for incoming documents (subset of Message without vector).

    3. `SearchResponse(BaseModel)`:
       - Structure for returning grouped results.
  </action>
  <verify>
    python3 -c "from src.app.schemas.message import Message; print(Message.model_fields.keys())"
  </verify>
  <done>
    Models are defined and importable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Embedding Service</name>
  <files>src/app/services/embedding.py</files>
  <action>
    Create `src/app/services/embedding.py`.
    - Class `EmbeddingService`.
    - Initialize `fastembed.TextEmbedding` with model `BAAI/bge-small-en-v1.5` (or `all-MiniLM-L6-v2` - 384 dim).
    - Method `embed_text(text: str) -> list[float]`:
      - Handles the embedding generation.
      - NOTE: This is CPU bound. The API layer will handle threadpool execution, but this service should just do the work.
    - Implement caching/singleton pattern so model loads only once.
  </action>
  <verify>
    python3 -c "from src.app.services.embedding import EmbeddingService; s = EmbeddingService(); print(len(s.embed_text('test')))"
    # Expect 384
  </verify>
  <done>
    Service generates 384-dimensional vectors from text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Database Client</name>
  <files>src/app/db/client.py</files>
  <action>
    Create `src/app/db/client.py`.
    - Function `get_db()` or `DBClient` class.
    - Connect to LanceDB at path specified in Settings.
    - Function `init_db()`:
      - Checks if "messages" table exists.
      - If not, creates it using the `Message` schema.
      - Creates FTS index on `content`.
      - Creates Vector index on `vector`.
  </action>
  <verify>
    python3 -c "from src.app.db.client import init_db; init_db()"
    # Check if directory exists
    ls -d data/wims.lance
  </verify>
  <done>
    Database initializes and table is created on disk.
  </done>
</task>

</tasks>

<verification>
Run a script that:
1. Initializes DB.
2. Embeds a string.
3. Inserts a record.
4. Queries the record.
</verification>

<success_criteria>
- [ ] `Message` schema is compatible with LanceDB
- [ ] Embedding service returns correct vector dimension (384)
- [ ] Database client creates table on startup
- [ ] FTS and Vector indices are configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-02-SUMMARY.md`
</output>
