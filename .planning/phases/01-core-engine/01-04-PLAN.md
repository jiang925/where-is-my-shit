---
phase: 01-core-engine
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - Dockerfile
  - src/static/test.html
  - scripts/verify_core.sh
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Container builds successfully"
    - "Embedding model is cached inside container"
    - "Container runs without internet access"
    - "Test page allows manual interaction"
  artifacts:
    - path: "Dockerfile"
      provides: "Container definition"
      contains: "RUN python"
    - path: "src/static/test.html"
      provides: "Manual testing interface"
  key_links:
    - from: "Dockerfile"
      to: "src/app/services/embedding.py"
      via: "Pre-download script"
---

<objective>
Package the Core Engine into a deployable artifact and verify all requirements.

Purpose: Deliver the "All-in-One" Docker container as requested in requirements (DEP-01, DEP-02).
Output: A buildable Docker image and verification tools.
</objective>

<execution_context>
@.planning/phases/01-core-engine/01-04-PLAN.md
</execution_context>

<context>
@.planning/phases/01-core-engine/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile with Model Caching</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    Create `Dockerfile`.
    - Base: `python:3.12-slim`
    - Install dependencies from `requirements.txt`.
    - **CRITICAL**: Add a build step to pre-download the embedding model.
      `RUN python -c "from fastembed import TextEmbedding; TextEmbedding(model_name='BAAI/bge-small-en-v1.5')"`
      (Ensure model name matches `services/embedding.py`).
    - Copy source code.
    - Expose port 8000.
    - CMD: `uvicorn src.app.main:app --host 0.0.0.0`

    Create `.dockerignore` to exclude `data/`, `__pycache__`, etc.
  </action>
  <verify>
    docker build -t wims-core .
  </verify>
  <done>
    Image builds and contains the model files (no download on startup).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Manual Test Page</name>
  <files>src/static/test.html, src/app/main.py</files>
  <action>
    1. Create `src/static/test.html`.
       - Simple HTML/JS UI.
       - Form to Ingest (Content, Platform).
       - Input to Search.
       - Display results as JSON.

    2. Update `src/app/main.py`.
       - Mount `src/static` to `/static`.
       - Redirect root `/` or add link to `/static/test.html`.
  </action>
  <verify>
    Open http://localhost:8000/static/test.html
  </verify>
  <done>
    Test page interacts with API successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Verification Script</name>
  <files>scripts/verify_core.sh</files>
  <action>
    Create `scripts/verify_core.sh`.
    - Shell script using `curl`.
    - Checks Health.
    - Ingests a known document ("The quick brown fox").
    - Searches for semantic match ("fast animal").
    - Asserts 200 OK and presence of ID in response.
    - Prints "VERIFICATION PASSED" on success.
  </action>
  <verify>
    chmod +x scripts/verify_core.sh
    ./scripts/verify_core.sh
  </verify>
  <done>
    Automated verification of the core engine functionality.
  </done>
</task>

</tasks>

<verification>
1. Build container: `docker build -t wims-core .`
2. Run container: `docker run -p 8000:8000 wims-core`
3. Disconnect internet (optional check).
4. Run verification script against container.
</verification>

<success_criteria>
- [ ] Docker image contains embedded model (startup is fast)
- [ ] Manual test page works
- [ ] Verification script passes against Dockerized app
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-04-SUMMARY.md`
</output>
