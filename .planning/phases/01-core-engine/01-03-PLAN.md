---
phase: 01-core-engine
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - src/app/api/v1/router.py
  - src/app/api/v1/endpoints/ingest.py
  - src/app/api/v1/endpoints/search.py
  - src/app/api/v1/endpoints/health.py
  - src/app/main.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can ingest documents via API"
    - "User can search documents via API"
    - "Search results are grouped by conversation"
    - "Health check returns detailed system stats"
  artifacts:
    - path: "src/app/api/v1/endpoints/search.py"
      provides: "Search logic"
      exports: ["search_documents"]
    - path: "src/app/api/v1/endpoints/ingest.py"
      provides: "Ingestion logic"
      exports: ["ingest_document"]
  key_links:
    - from: "src/app/api/v1/endpoints/ingest.py"
      to: "src/app/services/embedding.py"
      via: "run_in_threadpool"
    - from: "src/app/api/v1/endpoints/search.py"
      to: "src/app/db/client.py"
      via: "table.search(query, search_type='hybrid')"
---

<objective>
Implement the REST API surface for the Core Engine.

Purpose: Expose the system's capabilities (ingest, search, health) to the outside world (browser extension, UI).
Output: Fully functional API endpoints ready for consumption.
</objective>

<execution_context>
@.planning/phases/01-core-engine/01-03-PLAN.md
</execution_context>

<context>
@.planning/phases/01-core-engine/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Ingestion Endpoint</name>
  <files>src/app/api/v1/endpoints/ingest.py</files>
  <action>
    Create `src/app/api/v1/endpoints/ingest.py`.
    - Route: `POST /ingest`
    - Logic:
      1. Accept `IngestRequest` (content, metadata, etc.).
      2. Generate ID if missing (UUID).
      3. Call `EmbeddingService` to get vector (WRAP IN `run_in_threadpool` to avoid blocking loop).
      4. Construct `Message` model.
      5. Insert into LanceDB table.
      6. Return 201 Created with ID.
  </action>
  <verify>
    # Mock test
    # POST /ingest with dummy data
  </verify>
  <done>
    Documents can be added to the database via HTTP.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Search Endpoint</name>
  <files>src/app/api/v1/endpoints/search.py</files>
  <action>
    Create `src/app/api/v1/endpoints/search.py`.
    - Route: `POST /search`
    - Logic:
      1. Accept `SearchRequest` (query, limit, filters).
      2. Embed query text (in threadpool).
      3. Execute LanceDB Hybrid Search (`search_type='hybrid'`).
         - Apply `limit` (default 50 raw candidates).
      4. **Post-processing**:
         - Group results by `conversation_id`.
         - Select top matching snippets per group.
         - Filter out low relevance (score cutoff).
      5. Return paginated groups.
  </action>
  <verify>
    # Mock test
    # POST /search with query
  </verify>
  <done>
    Search returns grouped, relevant results.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Health Endpoint & Router Wiring</name>
  <files>src/app/api/v1/endpoints/health.py, src/app/api/v1/router.py, src/app/main.py</files>
  <action>
    1. Create `src/app/api/v1/endpoints/health.py`.
       - Route: `GET /health`
       - Use `psutil` to return memory/cpu usage.
       - Use DB client to return row count and DB size on disk.

    2. Create `src/app/api/v1/router.py`.
       - Include routers from ingest, search, health.

    3. Update `src/app/main.py`.
       - Include the V1 router under `API_V1_STR` prefix.
       - Call `init_db()` in startup lifecycle event.
  </action>
  <verify>
    curl localhost:8000/api/v1/health
  </verify>
  <done>
    All endpoints are wired up and accessible.
  </done>
</task>

</tasks>

<verification>
Start server.
1. POST /ingest (Ingest 3 messages, 2 from same convo).
2. POST /search (Search for keyword).
3. Verify result contains grouped conversation.
4. GET /health (Verify stats).
</verification>

<success_criteria>
- [ ] Ingestion endpoint handles async embedding correctly
- [ ] Search endpoint performs hybrid search and grouping
- [ ] Health endpoint reports physical resource usage
- [ ] API structure follows v1 conventions
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-03-SUMMARY.md`
</output>
