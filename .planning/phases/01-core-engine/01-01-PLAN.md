---
phase: 01-core-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/app/core/config.py
  - src/app/core/logging.py
  - src/app/main.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Application starts successfully"
    - "Configuration loads from environment variables"
    - "Logs are output in structured JSON format"
    - "Swagger UI is accessible at /docs"
  artifacts:
    - path: "src/app/main.py"
      provides: "FastAPI application entrypoint"
      exports: ["app"]
    - path: "src/app/core/config.py"
      provides: "Application settings"
      contains: "class Settings"
    - path: "requirements.txt"
      provides: "Project dependencies"
      contains: "fastapi"
  key_links:
    - from: "src/app/main.py"
      to: "src/app/core/config.py"
      via: "import Settings"
      pattern: "get_settings"
---

<objective>
Initialize the project structure, dependency management, configuration system, and structured logging for the WIMS Core Engine.

Purpose: Establish the foundational infrastructure required for the FastAPI application to run reliably and observably.
Output: A running FastAPI skeleton with JSON logging and environment-based configuration.
</objective>

<execution_context>
@.planning/phases/01-core-engine/01-01-PLAN.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Dependencies and Structure</name>
  <files>requirements.txt, .gitignore</files>
  <action>
    Create the project directory structure (`src/app/core`, `src/app/api`, etc.).
    Create `requirements.txt` with core dependencies:
    - fastapi, uvicorn[standard]
    - lancedb, fastembed
    - structlog, python-json-logger
    - pydantic-settings
    - psutil (for health checks)
    Create a standard `.gitignore` for Python/FastAPI projects (excluding venv, __pycache__, .env, *.db).
  </action>
  <verify>
    ls -R src
    cat requirements.txt | grep "fastapi"
  </verify>
  <done>
    Directory structure exists and dependencies are defined.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Configuration and Logging</name>
  <files>src/app/core/config.py, src/app/core/logging.py</files>
  <action>
    1. Create `src/app/core/config.py` using `pydantic-settings`.
       - Define `Settings` class with:
         - `PROJECT_NAME`: str (default "WIMS Core")
         - `API_V1_STR`: str (default "/api/v1")
         - `LOG_LEVEL`: str (default "INFO")
         - `DB_PATH`: str (default "data/wims.lance")
       - Implement `get_settings()` dependency.

    2. Create `src/app/core/logging.py`.
       - Configure `structlog` to output JSON logs.
       - Ensure compatibility with standard python logging (intercept handler).
  </action>
  <verify>
    python3 -c "from src.app.core.config import get_settings; print(get_settings().PROJECT_NAME)"
  </verify>
  <done>
    Configuration loads defaults and logging is configured for JSON output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Application Entrypoint</name>
  <files>src/app/main.py</files>
  <action>
    Create `src/app/main.py`.
    - Initialize FastAPI app with title from Settings.
    - Configure logging on startup using `core.logging`.
    - Add a root endpoint `GET /` returning `{"msg": "WIMS Core Engine Ready"}`.
    - Ensure Swagger UI is enabled (default behavior).
  </action>
  <verify>
    # Create a temporary test script to check import and app creation
    python3 -c "from src.app.main import app; print(app.title)"
  </verify>
  <done>
    App object is created successfully with correct settings.
  </done>
</task>

</tasks>

<verification>
Run `uvicorn src.app.main:app --reload --port 8000` (background) and curl localhost:8000.
Check logs are in JSON format.
</verification>

<success_criteria>
- [ ] `requirements.txt` contains all specified packages
- [ ] `src/app/core/config.py` handles environment variables
- [ ] `src/app/main.py` defines the FastAPI application
- [ ] Application starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine/01-01-SUMMARY.md`
</output>
