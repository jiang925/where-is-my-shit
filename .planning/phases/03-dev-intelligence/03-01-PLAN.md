---
id: "03-01-PLAN"
phase: "03"
wave: "01"
name: "Implement Core Watcher Service"
depends_on: []
files_modified:
  - "wims-watcher/requirements.txt"
  - "wims-watcher/src/main.py"
  - "wims-watcher/src/watcher.py"
  - "wims-watcher/src/state.py"
  - "wims-watcher/src/client.py"
  - "wims-watcher/src/parser.py"
autonomous: true
description: "Build the Python-based file watcher service that monitors Claude Code history logs."
---

# Plan 03-01: Implement Core Watcher Service

## Goal
Create the standalone `wims-watcher` Python application that monitors `~/.claude/history.jsonl`, tracks read position to survive restarts, and pushes new entries to the WIMS Core Engine.

## Tasks
<task_list>
<task>
<description>
Scaffold the `wims-watcher` project structure and dependencies.
- Create directory structure: `wims-watcher/src`
- Create `requirements.txt` with `watchdog`, `requests`
</description>
<files>
<file>wims-watcher/requirements.txt</file>
<file>wims-watcher/src/__init__.py</file>
</files>
</task>

<task>
<description>
Implement `state.py` to manage file cursors.
- Class `StateManager`
- Load/Save cursor position (file offset) to `~/.local/state/wims/watcher_cursor.json`
- Handle file truncation (reset cursor if current offset > file size)
</description>
<files>
<file>wims-watcher/src/state.py</file>
</files>
</task>

<task>
<description>
Implement `parser.py` to handle `history.jsonl` reading.
- Function to read from specific offset
- Handle partial lines (wait for newline)
- Parse JSONL entries
- Filter/Transform into WIMS schema
</description>
<files>
<file>wims-watcher/src/parser.py</file>
</files>
</task>

<task>
<description>
Implement `client.py` to communicate with Core Engine.
- `WimsClient` class
- Method `ingest(payload)` calling `POST http://localhost:8000/ingest`
- Simple error handling (log and retry later logic handled by main loop or simple retry)
</description>
<files>
<file>wims-watcher/src/client.py</file>
</files>
</task>

<task>
<description>
Implement `watcher.py` and `main.py` entrypoint.
- Use `watchdog.observers.Observer`
- Event handler for `FileModifiedEvent` on `history.jsonl`
- Main loop that initializes state, processes existing unread lines, then starts watching
</description>
<files>
<file>wims-watcher/src/watcher.py</file>
<file>wims-watcher/src/main.py</file>
</files>
</task>
</task_list>

## Verification
- [ ] `pip install -r wims-watcher/requirements.txt` works.
- [ ] `python3 wims-watcher/src/main.py` starts without error.
- [ ] Modifying a dummy test file triggers the watcher logic (visible in logs).
- [ ] Cursor file is created in `~/.local/state/wims/`.

## Must Haves
- Watchdog correctly identifies file updates.
- Cursor state persists across restarts.
- JSON parsing handles malformed/partial lines gracefully.
