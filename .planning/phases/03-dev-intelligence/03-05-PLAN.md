---
phase: 03-dev-intelligence
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - wims-watcher/src/sources/antigravity.py
  - wims-watcher/src/sources/cursor.py
  - wims-watcher/src/main.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Antigravity logs are detected and ingested"
    - "Cursor chat logs are detected and ingested"
  artifacts:
    - path: "wims-watcher/src/sources/antigravity.py"
      provides: "Antigravity log parser"
      contains: "class AntigravityWatcher"
    - path: "wims-watcher/src/sources/cursor.py"
      provides: "Cursor chat parser"
      contains: "class CursorWatcher"
  key_links:
    - from: "wims-watcher/src/main.py"
      to: "wims-watcher/src/sources/antigravity.py"
      via: "import and instantiation"
---

<objective>
Implement missing log watchers for Antigravity and Cursor IDE to close verification gaps.

Gaps closed:
- "Antigravity logs are automatically indexed (DEV-02)"
- "Cursor chat logs are automatically indexed (DEV-03)"
</objective>

<execution_context>
@.planning/phases/03-dev-intelligence/03-VERIFICATION.md
</execution_context>

<context>
@.planning/phases/03-dev-intelligence/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Antigravity Watcher</name>
  <files>wims-watcher/src/sources/antigravity.py</files>
  <action>
    Create `AntigravityWatcher` inheriting from `BaseWatcher`.

    Logic:
    - Watch `~/.antigravity/logs/` (or equivalent path from research).
    - Parse log lines (JSON or text).
    - Extract fields: timestamp, level, message, agent_id.
    - Map to `LogEntry` schema with source="antigravity".
  </action>
  <verify>
    Unit test: Feed sample Antigravity log line, verify `LogEntry` output.
  </verify>
  <done>
    Antigravity logs are correctly parsed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Cursor Watcher</name>
  <files>wims-watcher/src/sources/cursor.py</files>
  <action>
    Create `CursorWatcher` inheriting from `BaseWatcher`.

    Logic:
    - Locate Cursor state database (usually `~/.config/Cursor/User/workspaceStorage` or `state.vscdb`).
    - NOTE: This might require SQLite watching or polling if file events aren't reliable for DBs.
    - For MVP: Watch the `workspace.json` or equivalent history file if available.
    - If complex, implement a basic "last modified" poller for the chat history file.
    - Map to `LogEntry` schema with source="cursor".
  </action>
  <verify>
    Unit test: Feed sample Cursor chat data, verify `LogEntry` output.
  </verify>
  <done>
    Cursor chat logs are correctly parsed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register new watchers</name>
  <files>wims-watcher/src/main.py</files>
  <action>
    Import `AntigravityWatcher` and `CursorWatcher`.
    Instantiate them in the `main` loop/setup alongside `ClaudeWatcher`.
    Ensure configuration (paths) can be overridden via env vars.
  </action>
  <verify>
    Run `python src/main.py --help` or check startup logs to see all 3 watchers initializing.
  </verify>
  <done>
    All three sources are active.
  </done>
</task>

</tasks>

<verification>
Manual verification:
1. Generate activity in Antigravity.
2. Generate activity in Cursor.
3. Check WIMS Core logs for "source": "antigravity" and "source": "cursor".
</verification>

<success_criteria>
- [ ] AntigravityWatcher implemented
- [ ] CursorWatcher implemented
- [ ] Main entry point loads all watchers
</success_criteria>

<output>
After completion, create `.planning/phases/03-dev-intelligence/03-05-SUMMARY.md`
</output>
