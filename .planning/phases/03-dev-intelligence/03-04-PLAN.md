---
phase: 03-dev-intelligence
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - wims-watcher/src/client.py
  - wims-watcher/src/main.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Watcher stays alive when Core Engine is unreachable"
    - "Watcher retries failed ingestion requests"
    - "User sees clear connection status in logs"
  artifacts:
    - path: "wims-watcher/src/client.py"
      provides: "Resilient HTTP client"
      contains: "Retry(total=5"
    - path: "wims-watcher/src/main.py"
      provides: "Startup connection check"
  key_links:
    - from: "wims-watcher/src/client.py"
      to: "requests.adapters.HTTPAdapter"
      via: "mount('http://', adapter)"
---

<objective>
Address UAT failures regarding service connectivity and robustness.
Ensure the Watcher service handles Core Engine downtime gracefully without crashing, and implements retry logic for transient failures.

Gaps closed:
- "Watcher successfully ingests logs to Core Engine" (UAT Gap 1)
- "Ingested logs contain project metadata" (UAT Gap 2 - indirect fix via connectivity)
</objective>

<execution_context>
@.planning/phases/03-dev-intelligence/03-UAT.md
</execution_context>

<context>
@.planning/phases/03-dev-intelligence/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement robust HTTP client with retries</name>
  <files>wims-watcher/src/client.py</files>
  <action>
    Update the API client to use `requests.Session` with `HTTPAdapter`.
    Configure `urllib3.util.retry.Retry` with:
    - total=5 retries
    - backoff_factor=1 (exponential backoff)
    - status_forcelist=[500, 502, 503, 504]

    Ensure `ingest_log` method catches `ConnectionError` and `Timeout` exceptions, logging them as warnings instead of letting them crash the application.
  </action>
  <verify>
    Create a test script that attempts to ingest to a non-existent port.
    Verify it logs warnings/retries instead of crashing with uncaught exception.
  </verify>
  <done>
    Client retries on failure and handles connection errors gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add startup connectivity check</name>
  <files>wims-watcher/src/main.py</files>
  <action>
    Modify `main.py` startup sequence:
    1. Attempt to ping/connect to Core Engine health endpoint (or root).
    2. If unreachable, log a specific warning: "Core Engine not reachable at {url}. Watcher will retry ingestion, but ensure wims-core service is running."
    3. Do NOT exit; proceed to start watching (logs will be buffered/retried by client).
  </action>
  <verify>
    Run `make start` with Core Engine stopped.
    Check logs for the specific warning message.
    Verify service stays running.
  </verify>
  <done>
    User is informed of dependency status on startup.
  </done>
</task>

</tasks>

<verification>
Manual verification required:
1. Stop Core Engine.
2. Start Watcher.
3. Verify it stays running and logs retries.
4. Start Core Engine.
5. Verify subsequent logs are ingested.
</verification>

<success_criteria>
- [ ] Watcher service survives Core Engine downtime
- [ ] Connection errors are logged clearly
- [ ] Retries happen automatically for transient failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-dev-intelligence/03-04-SUMMARY.md`
</output>
