<plan>
  <id>08-01</id>
  <name>Fix JWT Timestamp Precision</name>
  <description>Fix the Off-by-one second bug in token validation</description>
  <wave>1</wave>

  <task type="auto">
    <title>Update Security Logic</title>
    <description>Modify src/app/core/security.py to handle float timestamps correctly</description>
    <files>
      <file>src/app/core/security.py</file>
    </files>
    <action>
      Edit `src/app/core/security.py` inside `get_current_user`.
      Change the revocation check:
      From: `if iat is None or iat < token_valid_after:`
      To: `if iat is None or iat < int(token_valid_after):`

      This ensures we compare seconds to seconds, ignoring the microsecond creation delay of the password reset.
    </action>
    <verify>
      <script>
        # Python script to verify fix
        import jwt
        import time
        from src.app.core.security import get_current_user
        # Mock auth db and token
      </script>
      <manual>
        1. Restart server
        2. Generate new token
        3. Curl /search endpoint
        4. Verify 200 OK (or 422 Validation Error, but NOT 401)
      </manual>
    </verify>
  </task>
</plan>
